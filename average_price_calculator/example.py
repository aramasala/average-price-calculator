"""Example helpers for notebooks and sample scripts."""


def hello(name: str) -> str:
    """Return a greeting string."""
    return f"Hello, {name}!"


def show_message(msg: str) -> None:
    """Print a message to stdout."""
    print(msg)
